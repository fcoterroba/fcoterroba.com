---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="404 - Página no encontrada | Page not found - fcoterroba"
  description="La página que buscas no existe. The page you're looking for doesn't exist."
  lang="en"
>
  <section class="min-h-screen flex items-center justify-center theme-bg py-20">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-12 text-center">
      <div class="mb-8 content-header">
        <h1 class="text-9xl sm:text-[12rem] font-bold text-brand-green mb-4" data-es="404" data-en="404">
          404
        </h1>
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold theme-text mb-6" data-es="página no encontrada" data-en="page not found">
          page not found
        </h2>
        <p class="text-lg sm:text-xl theme-text-80 max-w-2xl mx-auto mb-8" data-es="lo sentimos, la página que estás buscando no existe o ha sido movida." data-en="sorry, the page you're looking for doesn't exist or has been moved.">
          sorry, the page you're looking for doesn't exist or has been moved.
        </p>
      </div>

      <div class="my-12 flex justify-center gif-container">
        <div class="relative inline-block floating-gif">
          <img 
            src="/404-cat.gif" 
            alt="Confused cat looking at you"
            class="h-auto sm:h-auto rounded-2xl shadow-2xl"
            style="width: 300px; max-width: 90vw; aspect-ratio: 9/16;"
          />
          <div class="absolute inset-0 rounded-2xl bg-gradient-to-t from-black/20 to-transparent pointer-events-none"></div>
        </div>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center items-center mb-12">
        <a 
          href="/" 
          class="w-full sm:w-auto bg-brand-green text-white px-6 sm:px-8 py-3 sm:py-4 rounded-full text-base sm:text-lg font-semibold hover:bg-brand-green/90 transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-xl"
        >
          <span data-es="volver al inicio" data-en="go back home">go back home</span>
        </a>
        <a 
          href="/blog" 
          class="w-full sm:w-auto bg-transparent border-2 border-brand-green text-brand-green px-6 sm:px-8 py-3 sm:py-4 rounded-full text-base sm:text-lg font-semibold hover:bg-brand-green hover:text-white transition-all duration-300 transform hover:scale-105"
        >
          <span data-es="ir al blog" data-en="go to blog">go to blog</span>
        </a>
      </div>

      <div class="mt-12">
        <p class="theme-text-60 text-sm sm:text-base">
          <span data-es="¿necesitas ayuda? puedes contactarme" data-en="need help? you can contact me">
            need help? you can contact me
          </span>
          {' '}
          <a 
            href="/contacto" 
            class="text-brand-green hover:text-brand-green/80 transition-colors font-semibold"
            data-es="aquí" 
            data-en="here"
          >
            here
          </a>
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  .floating-gif {
    animation: float 4s ease-in-out infinite;
  }

  .content-header {
    animation: slideInDown 0.8s ease-out;
  }

  @keyframes slideInDown {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>

<script>
  import gsap from 'gsap';

  document.addEventListener('DOMContentLoaded', () => {
    const gifContainer = document.querySelector('.gif-container');
    const contentHeader = document.querySelector('.content-header');

    gsap.from(contentHeader, {
      opacity: 0,
      y: -30,
      duration: 0.8,
      ease: 'power2.out',
    });

    gsap.to('.floating-gif', {
      y: -15,
      duration: 3,
      ease: 'sine.inOut',
      yoyo: true,
      repeat: -1,
    });

    document.addEventListener('mousemove', (e) => {
      if (!gifContainer) return;
      
      const rect = gifContainer.getBoundingClientRect();
      const centerX = rect.left + rect.width / 2;
      const centerY = rect.top + rect.height / 2;
      
      const distX = (e.clientX - centerX) * 0.05;
      const distY = (e.clientY - centerY) * 0.05;
      
      gsap.to('.floating-gif', {
        x: distX,
        y: -15 + distY,
        duration: 0.3,
        overwrite: 'auto',
      });
    });

    const gifImg = document.querySelector('.floating-gif img');
    if (gifImg) {
      gifImg.addEventListener('mouseenter', () => {
        gsap.to('.floating-gif', {
          boxShadow: '0 0 30px rgba(76, 175, 80, 0.5)',
          scale: 1.05,
          duration: 0.3,
        });
      });

      gifImg.addEventListener('mouseleave', () => {
        gsap.to('.floating-gif', {
          boxShadow: '0 25px 50px -12px rgb(0 0 0 / 0.25)',
          scale: 1,
          duration: 0.3,
        });
      });
    }
  });
</script>

